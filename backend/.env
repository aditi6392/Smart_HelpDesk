# Server port
PORT=5000

# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/helpdesk

# JWT secret key (used for signing tokens)
JWT_SECRET=SuperSecretKey12345!@#

# Optional: token expiration (can be set in code)
JWT_EXPIRES_IN=30d
